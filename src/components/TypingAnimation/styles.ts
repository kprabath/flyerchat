import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    // position: 'absolute',
  },
  dotContainer: {
    justifyContent:'center', 
    alignContent: 'center',
    alignItems:'center',
    flexDirection: "row"
  },
})
